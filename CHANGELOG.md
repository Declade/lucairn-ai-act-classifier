# Changelog

All notable changes to `@lucairn/ai-act-classifier` will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- **Day 13 — npm publish dry-run + JSON Schema artifact + anchor blog post draft EN+DE + alpha-test prep + Day-12 LOW polish.**

  **npm publish dry-run gate.** New `scripts/verify-npm-tarball.sh` + `pnpm verify-tarball` npm script does a clean build + `pnpm pack` and asserts every load-bearing artifact (CLI binary + rules/lexicon JSON + locale labels + blog excerpts + JSON Schema + README + LICENSE + DATASET-LICENSE + CHANGELOG) is in the tarball. Also asserts examples/, accuracy/, test/, scripts/, node_modules/, and .git/ are NOT in the tarball (defense against `files` allowlist drift). `pnpm publish --dry-run` exits 0 with `+ @lucairn/ai-act-classifier@0.1.1` against the v0.1.1 tag-prep state. Day 14 will replace this with real `pnpm publish` once Marc authorises npm publication.

  **`dist/classify-result.schema.json` — JSON Schema artifact for ClassifyResult.** Hand-curated JSON Schema draft-07 description of the public `classify()` output shape. Lives in `dist/` and ships in the npm tarball (now listed explicitly in package.json `files` allowlist). The schema is informational — not used at runtime — but gives programmatic consumers (CI scripts, Python clients, etc.) a stable machine-readable description of what `classify()` returns. Generated by `scripts/generate-schema.ts` (invoked from the build chain after rules+lexicon copy steps); `$id` includes the package version so consumers can detect schema drift across releases. Hand-curation rather than auto-generation because `ts-json-schema-generator` trips on the `mode` template-literal type and the `three_category` null union; hand-curated `description` strings also cite EUR-Lex articles, which is what downstream consumers actually need. 6 new tests in `test/scripts/generate-schema.spec.ts` lock the contract: valid JSON, draft-07 marker, $id-version, every public `ClassifyResult` field appears in the schema's `required` array (drift signal vs src/classify.ts), informational-tool framing in the description.

  **CLI --help polish + actionable LLM_* error recovery hints.** Every `LLM_NO_API_KEY` error now ends with the exact `export VAR=...` command (not just a "See README" pointer) so users can copy-paste from the terminal. `LLM_SDK_NOT_INSTALLED` errors suggest both `pnpm add` AND `npm install` invocations and (for the openai provider) clarify "the same SDK serves both --llm openai and --llm groq" so a `--llm groq` user doesn't try to install a non-existent `groq-sdk` package. `LLM_API_ERROR` errors append a status-page URL (`status.anthropic.com` / `status.openai.com` / `groqstatus.com`; the OpenAI provider routes by inspecting `opts.baseURL` for the Groq path) so users distinguish transient outages from key/quota issues. `LLM_PARSE_ERROR` errors suggest a different provider via `--llm openai|groq` (anthropic) or `--llm anthropic|groq` (openai) plus a `--no-cache` retry. The API-key redaction invariant from Day 9 is unchanged: `redactApiKey()` runs BEFORE the status-page hint is appended. CLI --help text Examples + Exit codes sections were already in place from Day 11; new test in test/cli.spec.ts locks both sections + all 4 exit codes (0/1/2/3) against future regressions.

  **`examples/blog-draft/eu-ai-act-classifier-launch.{en,de}.mdx` — anchor blog post draft.** 2732-word English draft + 2576-word natural-German draft (hand-written, NOT machine-translated). Title per canonical plan outline: *"We Built a Free CLI That Maps Any AI Use Case to the EU AI Act Articles It Triggers. Here's What 50 Real Cases Taught Us."* 7-section structure: (1) the problem (compliance teams burn 6-10 hours per classification), (2) what the tool does (with real CLI output block), (3) the locked three-category obligation mapping (Cat 1 sanitizer / Cat 2 evidence / Cat 3 inventory), (4) five worked examples with REAL `dist/cli.js` outputs (employment screening Annex III ¶4(a) / law-enforcement Art 5(1)(h) prohibition / GPAI fine-tuning Art 50(1) / deepfake Art 50(4) / product-recommendation negative), (5) what the 50 test cases revealed — accuracy numbers cited verbatim from `accuracy/REPORT.md` (98.2% overall / 100% Article 5 / 98.0% binary high-risk deterministic + 93.5-97.6% LLM Anthropic range) + three specific Day-7 fixtures (fixture-09 task-allocation-DE, fixture-11 emergency-triage-DE, fixture-17 election-influence-EN) cited as the "looks operational; actually high-risk" pattern, (6) limitations — credibility move; cites G-5 fixture-28 compound-noun gap from `accuracy/KNOWN-MISCLASSIFICATIONS.md` as concrete example of honest disclosure, (7) use it / fork it / break it — cross-links 3 existing AI Act blog posts + 3 existing `/tools/` pages on lucairn.eu; NO sales CTA. Companion `eu-ai-act-classifier-launch.README.md` ships the Marc-facing publish workflow + suggested SEO meta (title ≤70 chars, description ≤160 chars, 8 keywords in priority order) + target-audience anti-patterns + polishing checklist for Day-14 morning.

  **`docs/ALPHA-TESTING.md` — internal alpha-test prep for 3-5 EU/DE consultants.** 5 representative test cases × 2 locales: employment screening (Annex III ¶4 + cascade), predictive policing with Art 5(1)(d) disambiguator ("ausschließlich auf Profiling der natürlichen Person"), asylum-application processing (Annex III ¶7(c) "looks operational; actually high-risk"), deepfake-generation deployer (Art 50(4) sub-1 vs sub-2 distinction), negative case weather-forecast AI. Each case has verbatim input text in both languages + expected classification table + EUR-Lex citation. 4-question feedback collection template per case (Q1 classification correctness / Q2 explain defensibility / Q3 workflow gap / Q4 LEGALLY WRONG escalation) + 5 overall questions. Marc-facing recipe documents target distribution (Bitkom WG + BfDI working group + named LinkedIn contacts; explicit anti-targets: day-job clients, active sales prospects, hostile-to-regulation contacts), personal-email template, synthesis deadline (Day-14 morning), and three decision rules (proceed / partial-publish-with-disclosure / hold). Explicit "what this is NOT" disclaimer rules out marketing-exercise framing.

  **Day-12 deferred LOW polish bundled in.** Closes review-pass L3 (`data-plausible-event` placeholder polish in examples/hosted-ui/ClassifierClient.tsx): the attribute was the wrong form — vanilla Plausible script reads class-based custom events per [official docs](https://plausible.io/docs/custom-event-goals), not data-attributes. Replaced with `class="plausible-event-name=classify-submit"` + documented the class-vs-data-attribute distinction inline + updated examples/hosted-ui/README.md + examples/hosted-ui/page.tsx comments to match. Closes L4 (CHANGELOG wording cleanup — current entry serves). Closes N1 (workspace test comment accuracy): the test/integration/workspace-import.spec.ts comment listed `"ranks candidates"` as a lexicon match but the canonical lexicon phrase is `"candidate ranking"`; corrected + added precise cite-back to test/fixtures/use-cases/day3/04-employment-en.json:4.

  **Banner v0.1.1 / Day 13 in both locales.**

  **Fix-up round 1 bundled.** Closes 1 BLOCKER + 5 citation-verification FAILs + 1 review-pass HIGH + 4 review-pass MEDIUMs + 5 personal-info-leak scan WARNs + 1 claim-enforcement review WARN.
  - **B1 BLOCKER closure — JSON Schema regenerated to match real ClassifyResult shape.** The previous schema was empirically broken (382 Ajv errors against real `classify()` output) because the generator emitted wrong shapes for nearly every nested type and the existing test only verified top-level required-field names — a tautology. Schema sub-shapes corrected: `features.byCategory` is now 2-level nested (`Record<group, Record<category, phrase[]>>`); `features.hits[]` carries `{group, category, phrase, source}` only; `article_5.hits[]` shape matches `src/rules/article-5.ts::Article5Hit` (8 letters a-h, `category_key`, `matched_phrases`, summaries, source) and `article_5` carries `reasoning: string[]`; `annex_iii` carries `{high_risk, suppressed_by_article_5, domains, reasoning}` with each domain having `{annex_iii_number, key, sub_letters, matched_phrases, title_en, title_de, source}`; Articles 10/12/13/14/15 use `triggered_by: {article_5: boolean, annex_iii_domains: integer[]}` (NOT a string array) + `summary_en`/`summary_de`/`source` (NO `rationale`); Article 50 has 5-boolean `triggered_by` + verbatim chapeau summaries (NO `paragraphs` array); `three_category` is either `null` OR `{categories: {1,2,3}, applicable_categories: string[], disclaimer_en, disclaimer_de, source}` (note: `applicable_categories` items are STRING keys, NOT integers). Added `ajv@^8` as devDependency. New empirical Ajv-validation test runs 5 representative fixtures (no-trigger, Annex III high-risk EN+DE, Article 5 prohibition, Article 50 deepfake) plus a fixture with `threeCategory: false` returning null. The previous top-level required-fields test is retained but its comment is rewritten to honestly note what it does and does not catch.
  - **Citation FAIL-1 (page count 458→144):** EUR-Lex OJ L EN+DE PDF for Regulation (EU) 2024/1689 is 144 pages, verified via `pdfinfo`. The blog front paragraph claimed "approximately 458 pages" / "rund 458 Seiten" — wrong by 3×. Both locales corrected.
  - **Citation FAIL-2 (Art 5(1)(d) DE verbatim):** inside the scare-quoted block, replaced `Persönlichkeitsmerkmale` → `persönlichen Merkmale und Eigenschaften` + `des Profilings` → `des Profiling`. Same drift class Day-11 closed in the blog excerpts. The lexicon's colloquial-paraphrase coverage (Profilings/nur/allein) is unchanged.
  - **Citation FAIL-3 + FAIL-4 (Annex III ¶4(b) + ¶5(d) DE):** scare-quoted EUR-Lex DE inside-quote blocks had 5 + 6 substantive drifts respectively (Beziehungen vs Arbeitsverhältnissen; Disposition vs Entsendung; medizinischer Hilfe vs medizinischer Nothilfe; Patiententriage-Systeme vs Systeme für die Triage von Patienten bei der Notfallversorgung; etc.). Replaced with parenthetical paraphrases that use EUR-Lex DE key terms (Arbeitsverhältnisse, Beobachtung und Bewertung, Beförderungen und Kündigungen, Entsendung, Priorisierung, medizinische Nothilfe). Verbatim discipline preserved where applied; paraphrase framing is honest about its status.
  - **Citation FAIL-5 (Art 50(4) sub-paragraph 1 vs sub-paragraph 2):** the blog Example 4 + alpha-test Case 4 framed sub-2 (editorial-review carve-out) as if it applied to video deepfakes. WRONG: sub-1 covers image/audio/video deep fakes (artistic/creative carve-out only); sub-2 covers AI-generated TEXT on matters of public interest (editorial-review carve-out applies to news article text, not the deepfake video itself). Empirically the classifier already routes Example 4's video input to sub-1 alone; the bug was in the docs. Both blog drafts (EN+DE) and alpha-test Case 4 rewritten with the correct framing.
  - **Review-pass HIGH H1 + MEDIUMs M1 + M4 (DE lexicon-aligned inputs):** the DE inputs in blog Beispiel 1 + alpha-test Case 1 DE + Case 3 DE used phrasings that fell outside the v0.1.1 lexicon / sub-letter-narrowing tables, so the CLI produced different output than the docs claimed (blog Beispiel 1 + Case 1 DE: only `einstellungsentscheidungen` matched, sub-letters empty, docs claimed 4(a); Case 3 DE: `Asylantrags` genitive not in lexicon, no high-risk fire, docs claimed 7(c)). Rewrote each DE input to use lexicon-aligned phrasing (`Lebenslauf-Screening` + `Bewerberauswahl` + `Einstellungsentscheidungen`; `jeden Asylantrag`). Each rewritten input was run through `classify()` and confirmed to produce the claimed narrowing before this commit landed. The morphology gap (genitive plural fallback) is a known v0.2 lexicon-curation follow-up.
  - **Review-pass MEDIUM M2 (Examples 3-5 honest framing):** the blog preamble claimed every example output was "real `dist/cli.js` output, not an illustration", but Examples 3-5 are abridged to 1-5 lines (real CLI emits ~18 lines). Rewrote the preamble (EN+DE) to honestly frame Examples 1+2 as full verbatim output and Examples 3-5 as load-bearing-lines-only abridgement, with a pointer to run the CLI for the full mapping.
  - **Review-pass MEDIUM M3 (alpha-test Case 4 column split):** the Three-category row mixed classifier output ("Limited risk; not applicable") with a Lucairn best-practice opinion ("Cat 2 may still be advisable") in one column. Split into "Classifier output" + "Best-practice consideration (NOT classifier output)" so testers do not flag the opinion as a classifier disagreement.
  - **Personal-info-leak scan W1 + W2 + W3 (internal reviewer-agent names on public-by-Day-14 surfaces):** replaced 18+ references to internal reviewer-agent identifiers across CHANGELOG.md + the two blog drafts + docs/ALPHA-TESTING.md with generic descriptive language ("citation-verification step", "review-pass HIGH/MEDIUM", "personal-info-leak scan", "claim-enforcement review"). Verified clean across all 4 files.
  - **Claim-enforcement review W-1 (package.json author field):** `"author": "Declade"` → `"author": "Marc Schülke <marc@lucairn.eu> (https://lucairn.eu)"` to match the live Impressum at lucairn.eu/{en,de}/impressum (Day-11 entity-name lock: Marc Schülke as natural-person operator; Lucairn UG i.Gr. as planned company). Declade is the GitHub-organisation name, not the legal entity.
  - **Review-pass LOWs L1 + L2 + L3 + L4 bundled:** L1 dropped duplicate `src/content/blog-excerpts/**/*.md` glob from package.json's `files` allowlist (`dist` glob already includes the built copy; verify-tarball still asserts the schema + excerpts are present); L2/N1 corrected the workspace-import test comment to honestly describe which lexicon entries actually substring-match the test input; L3 replaced 6 stale `README §--llm mode setup` pointers across src/cli.ts + 3 provider files with the literal README header `Setup for --llm mode`; L4 added inline comment to scripts/generate-schema.ts noting that `$id` is a stable versioning marker, NOT a fetchable URL (consumers load the schema from `dist/classify-result.schema.json` in the npm tarball). N2 (redactApiKey exact-match coverage) + N3 (publish workflow nit) deferred to v0.2.
  - **Tests:** 657 baseline → 659 (+2 new empirical Ajv tests in `test/scripts/generate-schema.spec.ts`). Deterministic accuracy unchanged (98.2% overall / 100.0% Art 5 / 98.0% binary high-risk). RULES_HASH unchanged (`12eafb8a` — no rule or lexicon JSON edits).

- **Day 12 — `package.json` `exports` field + hosted-UI example template + Day-11 deferred LOW polish.**

  **`exports` field for Next.js ESM workspace resolution.** Two-key shape (`"."` → `{types, import}` + `"./package.json"`) added to `package.json`. Preserves the existing `main` + `types` fields for tools that don't read `exports` (backward compatible). Unlocks the Day-12 Part B Marc-action — copying `examples/hosted-ui/` into `theveil-website/src/app/[lang]/tools/ai-act-classifier/` and adding `@lucairn/ai-act-classifier` as a workspace dependency. Without this field, a Next.js parent app importing the package would resolve to the CommonJS shape or fail entirely.

  **`examples/hosted-ui/` template directory.** Ready-to-copy Next.js page template at `examples/hosted-ui/{page.tsx, ClassifierClient.tsx, server-action.ts, README.md}`. Self-contained: imports `classify` + `formatExplain` from `@lucairn/ai-act-classifier` directly as a workspace dependency (no `ToolWorkspace` discriminator change required). Server-side classify — the lexicon + rules JSON never reach the browser bundle. Deterministic mode only (LLM mode is a v0.2 hosted-UI feature). 8 KiB input cap enforced at the server-action boundary. `data-plausible-event="classify-submit"` attribute on the submit button wires up site analytics. `TODO(i18n)` + `TODO(theveil-website integration)` markers flag the swaps Marc applies during Part B (replace `Metadata` block with `buildPageMetadata`; replace inline EN/DE strings with theveil-website's i18n loader). The classifier repo does NOT install `next` or `react` deps — the template ships as source-only with a single `@ts-expect-error` directive on the `Metadata` import that disappears at integration time. Both `pnpm typecheck` and `pnpm typecheck:test` exclude `examples/`.

  **Workspace-import smoke test** at `test/integration/workspace-import.spec.ts` (4 new tests). Proves the dist build is end-to-end importable via the pnpm self-import path: asserts `classify` + `formatExplain` resolve, runs a real classification call against the Annex III ¶4 employment fixture phrasing, and renders a markdown explain output. Falls back to a direct `file:` URL import of `dist/index.js` if the self-import path is unavailable (sandbox-EPERM-friendly).

  **Day-11 deferred LOW polish bundled in.** Closes review-pass L1 (README cache-mode wording: clarified `0o600` / `0o700` as POSIX bit semantics in both locales) + L2 (explain.ts cite-implicit disclosure: new header comment enumerating the upstream sources `src/util/citations.ts::getCitation()` and `src/i18n/{en,de}.json::annex_iii_chapeaux + ::annex_iv_chapeau`, plus the "never-inline-a-citation" maintainer discipline). L4 spot-checked clean (README line 50 chapeau claim is accurate post-Day-11 FX4 — the Annex III/IV chapeaux render from the i18n constants, not paragraph titles). Article 10 DE `in denen` verified clean from the Day-11 fix-up. Other Day-11 deferred cosmetic regulator WARNs (verbatim-quote-drift on Article 10 DE minor, Art 5(1)(d) DE genitive context) remain deferred to v0.2 polish per the Day-11 handover.

  **Banner v0.1.1 / Day 12 in both locales.**

  **Fix-up round 1 bundled.** Closes personal-info W-1 + W-2 (canonical-plan path + `deploy@` script-path leak in `examples/hosted-ui/README.md`) + claim-enforcement review M-1 (same finding) + 3 review-pass MEDIUMs: (a) `server-action.ts` `text.length` byte cap was UTF-16-counted, now `new TextEncoder().encode(text).byteLength` for accurate UTF-8 enforcement (German umlauts would have let ~16 KiB DE payloads through an "8 KiB" cap unnoticed); (b) production-caveat sub-section added to the architecture notes documenting Next.js's `next start` server-action error-stripping behaviour + the recommended discriminated-union return-shape refactor for Part B integration; (c) `src/data/citations.json` `service_desk_*` field rename to `regulation_text_mirror_*` to match Day-11 FX6 12-surface Tier-3 relabel pattern (URLs unchanged — the field name implied Tier-1 sourcing but the URL set was always the FLI Tier-3 mirror). Plus one L1 polish at `src/format/explain.ts:55-56` aligning the upstream-source comment with the post-FX4 Tier naming. URLs are byte-identical so all explain / cli-table / markdown snapshots pass without regen. citations.json is NOT in the rules-hash set so RULES_HASH stays at `12eafb8a`.

  Tests: 646 → 650 (+4 workspace-import). Deterministic accuracy unchanged (98.2% overall / 100.0% Art 5 / 98.0% binary high-risk). RULES_HASH unchanged (`12eafb8a` — no rule or lexicon JSON edits).

  **What this PR does NOT do.**
  - Does NOT touch theveil-website. Day-12 Part B (the actual integration at `theveil-website/src/app/[lang]/tools/ai-act-classifier/page.tsx`) is a Marc-action; the template directory + integration README is the orchestrator-shipped scaffold.
  - Does NOT publish to npm. Day-14 will run `pnpm publish` after the public-flip.
  - Does NOT change the rules engine, extractor, providers, or cache.
  - Does NOT modify accuracy numbers.

- **Day 11 PR #11 fix-up round 1 — reviewer-chain closures + content-drift corrections.** Closes 4 citation-verification FAILs + 4 review-pass MEDIUMs + 2 claim-enforcement WARNs + 1 personal-info WARN + 4 of 8 verbatim-quote-drift WARNs (Annex III ¶4 EN gerund-vs-infinitive + ¶4 DE Beobachtung + Art 10 DE in denen + ¶6 DE Verlässlichkeit). Notable: (a) the `--explain` markdown output's Annex IV section had the chapeau and rationale fields swapped — DPIA evidence reviewers saw the classifier's internal cascade reasoning where the verbatim regulation text should have been. Closed via `formatExplain()` field-rendering swap + new verbatim-chapeau constants in `i18n/{en,de}.json::annex_iv_chapeau`. (b) Similarly the Annex III section rendered paragraph titles in the chapeau slot instead of the verbatim EUR-Lex paragraph chapeaux (the title alone omits the "in so far as their use is permitted under relevant Union or national law" carve-out wording that lives in ¶¶1/6/7 chapeaux); closed via new `i18n/{en,de}.json::annex_iii_chapeaux` table keyed by ¶ number. (c) README disclaimer entity was "Declade UG (i.G.)" — contradicted the live Impressum at lucairn.eu/{en,de}/impressum which names "Marc Schülke" as the current natural-person operator and "Lucairn UG (haftungsbeschränkt) i.Gr." as the planned company; corrected in both READMEs. (d) Annex III ¶6(e) EN+DE content was "crime analytics across criminal data" — wrong; EUR-Lex ¶6(e) is "profiling of natural persons under Article 3(4) of Directive (EU) 2016/680 in the course of detection, investigation, or prosecution of criminal offences"; corrected. (e) Cache-dir mode test was a tautology `expect(perms & 0o700).toBe(perms & 0o700)`; replaced with proper invariant assertion. (f) `--explain` CLI gained 6 spawnSync integration tests covering markdown/json/text output, `--with-excerpt` excerpt-block appending, `--format json` silent-override behaviour, and exit-code 1 on Art 5 prohibition. (g) `--explain` overrides `--format` / `--json` documented in CLI `--help` text + README EN+DE. (h) 12-surface relabel of artificialintelligenceact.eu references — formerly cross-labelled as "EU AI Office service desk" but actually a Tier-3 Future of Life Institute mirror (the EU AI Office's official Service Desk is at ai-act-service-desk.ec.europa.eu/europa.eu). README citation block reorganised by tier (Tier 1: EUR-Lex + EU AI Office Service Desk; Tier 2: BSI + BfDI + Bitkom; Tier 3: FLI mirror). Internal citation-verification agent name on README rewritten as "automated citation-verification step". Test count: 634 (Day-11 ship) → 646 (+6 explain + 6 cli --explain spawnSync; net 0 cache spec change since tautology was replaced in-place). RULES_HASH unchanged.

- **Day 11 — `--explain` flag + blog-excerpt corpus + README EN+DE launch finalization + Day-10 LOW carry-forward closures.**

  **New `--explain` flag** lights up the rules-first architecture's defensibility surface. For every fired article the formatter emits the verbatim EUR-Lex chapeau + matched lexicon phrases + sub-letter narrowing branch + a 1-line "why this paragraph fires" rationale + the Tier-1 citation URL. The output is consultant-paste-friendly markdown by default; `--explain-format json` returns the same content as a snapshot-stable structured object for programmatic consumers; `--explain-format text` returns plain ASCII for terminal pipes. Three additional reasoning surfaces: (a) **disambiguator state** — surfaces the Art 5(1)(d) "solely on profiling" / "ausschließlich auf Profiling" qualifier check explicitly, including the NOT-fired branch when the lexicon hits predictive policing without the disambiguator (the rule routes to Annex III ¶6 high-risk; we surface this transition); (b) **nearest-miss reasoning** — up to 2 articles considered but not fired, including cascade-suppressed-by-Article-5 and Art 2(8) research-only carve-out cases; (c) optional **commentary excerpt block** via `--with-excerpt` that appends a hand-curated regulator-explainer paragraph from `src/content/blog-excerpts/<key>.<locale>.md`. The excerpt loader is graceful: missing files are silently omitted (never throws); the lookup keys are stable (cite-back: `src/format/explain.ts:getExcerptKey()`). New CLI flags: `--explain`, `--explain-format <markdown|json|text>` (default `markdown`), `--with-excerpt`. `--explain` overrides `--format` / `--json`.

  **New blog-excerpt corpus** at `src/content/blog-excerpts/` (5 keys × EN+DE = 10 files): article-5-1-d-predictive-policing, annex-iii-4-employment, annex-iii-6-law-enforcement, article-10-data-governance, article-50-transparency. Every file is 80-150 words of hand-curated original prose citing at least one Tier-1 source (EUR-Lex, EU AI Office, BSI, BfDI, or Bitkom). **NOT machine-translated** — the DE files are hand-written natural German per consultant judgment. License: MIT, same as source code. The build script copies them into `dist/content/blog-excerpts/` so they ship in the npm tarball.

  **README EN + DE finalized for launch.** Both READMEs restructured for the Day-14 public-launch reading: 3+ usage examples at the top (deterministic + `--explain` + `--explain --with-excerpt` + `--llm`), refined "What it does" + "What it does NOT do" non-claims, refined `--explain` documentation with output-format matrix, refined cache-layer disclosure (mode 0600 + lexicon-version + prompt-checksum invalidation), refined Accuracy + Methodology + Citations sections cross-linking [accuracy/METHODOLOGY.md](./accuracy/METHODOLOGY.md) + [accuracy/KNOWN-MISCLASSIFICATIONS.md](./accuracy/KNOWN-MISCLASSIFICATIONS.md), refined disclaimer (informational tool / not legal advice / Lucairn-not-in-data-path framing) consistent across both locales. Banner v0.1.1 / Day 11. Hosted-UI cross-link to `https://lucairn.eu/tools/ai-act-classifier` (live Day 13).

  **Day-10 LOW carry-forward closures bundled.**
  - **L4 (cache file perms):** `src/extract/cache.ts` `writeFile` now passes `{mode: 0o600}` and `mkdir` passes `{mode: 0o700}`. README EN+DE cache-layer section notes the perms explicitly. New test asserts `fs.statSync(cacheFile).mode & 0o777 === 0o600`.
  - **L6 (DEFAULT_MODEL drift):** each provider module now exports `DEFAULT_MODEL` as a named export; `src/extract/llm.ts:getDefaultModel()` imports them rather than duplicating literals. New drift-guard test asserts equality across all 3 providers.
  - **L7 (internal-export coverage gap):** new tests in `test/extract/cache.spec.ts` covering `normalizeInputForCacheKey()` (idempotence + whitespace-collapse + non-lowercasing) and `getDefaultModel()` (returns correct string per provider).

  **Tests** — 41 new in `test/format/explain.spec.ts` covering input validation + 4 representative classify() output snapshots × 3 format variants (Annex III ¶4 employment EN, Art 5(1)(d) DE prohibition, Article 50 deepfake EN, negative weather model EN) + locale switching + nearest-miss rationale correctness + disambiguator-state surfacing in EN+DE + `--with-excerpt` graceful-omission + disclaimer-footer invariant + `getExcerptKey()` mapping table. 46 new in `test/content/blog-excerpts.spec.ts` covering file inventory + key parity + Tier-1 citation invariance + banned-literal sweep + content-discipline (byte-size range). Plus ~10 new tests for the L4/L6/L7 closures. Test count: 533 (Day-10) → 620+ (Day-11).

  **What this PR does NOT do.**
  - Does NOT modify the rules engine or extractor — `--explain` is presentation-layer only.
  - Does NOT change `RULES_HASH` — no rule or lexicon JSON edits.
  - Does NOT add LLM-mode-specific `--explain` differences — same output shape regardless of extraction mode.
  - Does NOT implement website-sync pipeline (Day-12 hosted UI is a separate workstream).
  - Does NOT change deterministic or LLM-mode accuracy numbers.

- **Day 10 — `--llm openai` + `--llm groq` modes + LLM-mode filesystem cache.** Three-provider story now LIVE:
  - `--llm openai` (default model GPT-4o-mini at ~\$0.0005/call; ~\$0.025 per 50-fixture run).
  - `--llm groq` (default model Llama 3.3 70B Versatile at ~\$0.0014/call; ~\$0.07 per 50-fixture run; uses the OpenAI SDK against `api.groq.com/openai/v1` — same chat-completions surface, lower cost, faster latency).
  - `openai@^6.0.0` added to `optionalDependencies` (Groq reuses the SAME SDK). Anthropic SDK already in optionalDependencies from Day 9.
  - New files: `src/extract/providers/openai.ts` (gpt-4o-mini with `tool_choice` forcing `emit_features` function call + `baseURL`/`apiKey` overrides load-bearing for the Groq reuse path), `src/extract/providers/groq.ts` (thin wrapper that dispatches into the OpenAI provider with `baseURL`=`https://api.groq.com/openai/v1` + Groq-specific model + Groq's own `GROQ_API_KEY` passed EXPLICITLY to the OpenAI client constructor so OPENAI_API_KEY env is NEVER polluted on Groq runs), `src/extract/providers/_shared.ts` (refactored shared bookkeeping — lexicon load/projection/index, hallucination guard, source-URL mapping, API-key redaction, system prompt + user-message builder — both providers honor the SAME citation-moat invariants).
  - `src/extract/providers/anthropic.ts` refactored to use the shared helpers; all 19 existing Day-9 tests pass byte-stable (pure refactor, no behaviour change).
  - **LLM-mode filesystem cache** at `~/.cache/lucairn-ai-act-classifier/llm/` (respects `XDG_CACHE_HOME`). Cache key = `sha256(provider + model + lexiconVersion + lang + inputNormalized)`. Atomic writes via `<key>.<random>.tmp` → `rename`. Tolerant reads (any fs / JSON error → cache miss, no throw). Failed API calls are NEVER cached. Cache miss ~1.4s → cache hit ~80ms (live-verified: 17.5× speedup). Automatic invalidation on lexicon-version bump (the version is part of the key).
  - New `--no-cache` CLI flag forces a fresh API call (cache neither read nor written for that invocation).
  - `extractFeaturesLLM` signature extended with `opts.cache?: CacheOptions` (`{ disabled?: boolean; cacheDir?: string }`). `classify(opts)` accepts the same `cache` parameter via passthrough.
  - New `pnpm accuracy:llm-openai` + `pnpm accuracy:llm-groq` scripts. Both gracefully emit a "skipped — no API key" placeholder report when the respective env var is unset (mirrors the Day-9 anthropic pattern).
  - New integration specs at `test/extract/llm-openai-integration.spec.ts` + `test/extract/llm-groq-integration.spec.ts` (gated on `LUCAIRN_LLM_INTEGRATION=1` + respective API key; SKIPPED by default and during the Day-10 dispatch verification gate; per-run cost roughly \$0.0015 OpenAI, \$0.004 Groq).
  - **Tests** — 49 new unit tests: 21 in `test/extract/providers/openai.spec.ts` (full mocked SDK coverage: happy paths, hallucination drop, parse-error retry, key redaction, abort signal, baseURL propagation, JSON-string `function.arguments` parsing); 9 in `test/extract/providers/groq.spec.ts` (Groq baseURL/model/apiKey contract + OPENAI_API_KEY env-not-polluted invariant + transitive hallucination filtering); 18 in `test/extract/cache.spec.ts` (key determinism + 5-dimension invalidation + round-trip + tolerant-corrupt-file + atomic-write + XDG_CACHE_HOME resolution); 1 new CLI test (--help mentions --no-cache); 1 new llm.spec.ts test (openai/groq dispatch routing to LLM_NO_API_KEY). 6 new gated integration tests skip-by-default. Test count: 463 (Day-9) → 512 + 10 skipped = 522 (+55, of which 6 skip without env vars).
  - **Stable error-code prefixes** unchanged from Day 9: `LLM_NO_API_KEY:`, `LLM_SDK_NOT_INSTALLED:`, `LLM_UNKNOWN_PROVIDER:`, `LLM_PROVIDER_NOT_IMPLEMENTED:`, `LLM_NO_TOOL_USE:`, `LLM_PARSE_ERROR:`, `LLM_API_ERROR:`. The API key is redacted from any wrapped error message for all three providers.
  - README EN + DE updated: the single-provider `## --llm anthropic mode` section is now `## --llm mode (opt-in, 3 providers)` with a cost-comparison table (Anthropic Haiku 4.5 / OpenAI gpt-4o-mini / Groq Llama 3.3 70B) + per-provider setup snippet. New `## Cache layer` section documents the `~/.cache/lucairn-ai-act-classifier/llm/` storage, the cache key, `--no-cache` opt-out, and manual-clear instruction.

  **Architectural decision: Groq passes its API key via the OpenAI client constructor's `apiKey` parameter — NEVER via OPENAI_API_KEY env mutation.** This means a Groq run on a host where BOTH GROQ_API_KEY and OPENAI_API_KEY are set will use the Groq key exclusively + never consult the OpenAI key. Locked by a `OPENAI_API_KEY NOT polluted by Groq run` invariant test in `test/extract/providers/groq.spec.ts`.

  **What this PR does NOT do:**
  - Does NOT run real-API accuracy harness for OpenAI / Groq during the dispatch (the local env-file used by the dispatch harness carries ANTHROPIC_API_KEY only; OPENAI_API_KEY and GROQ_API_KEY have to be positioned explicitly to run those providers' integration tests).
  - Does NOT add a CI gate on cache behaviour (the cache is filesystem-state; CI runs are ephemeral).
  - Does NOT change RULES_HASH or any rule/lexicon JSON (no behaviour change to article selection).

- **Day 9 — `--llm anthropic` mode (opt-in LLM feature extraction via Claude Haiku 4.5).** Lights up the `--llm <provider>` CLI flag for `anthropic` only. Day 10 will add `openai` + `groq` + a cache layer. The LLM only extracts features — the rules engine that selects articles is unchanged. The LLM is constrained to cite phrases from the curated lexicon; any hallucinated phrase is dropped before the rules engine sees it (architectural guard against bypassing the citation moat). New files: `src/extract/llm.ts` (provider-dispatch scaffold with dynamic-import contract), `src/extract/providers/anthropic.ts` (Anthropic Haiku 4.5 provider with tool-use structured output + inline schema validation + lexicon-phrase filtering + API-key redaction in errors), `src/extract/providers/index.ts` (barrel). `@anthropic-ai/sdk@^0.30.1` added to `optionalDependencies` — the default install does not pull it; `pnpm add @anthropic-ai/sdk` is required for `--llm anthropic` mode. `classify()` is now async (`Promise<ClassifyResult>`) so the LLM path lands without a breaking signature; deterministic mode `await` resolves immediately (no I/O, no network). `result.mode` emits `'llm-anthropic'` when `--llm anthropic` is set. CLI exit code 3 is now LIVE for LLM errors (no API key, SDK not installed, parse error after retry, network error); other errors still exit 2. New `pnpm accuracy:llm-anthropic` script generates `accuracy/REPORT.llm-anthropic.md` (separate from deterministic `accuracy/REPORT.md`); LLM-mode is opt-in and NOT a CI-blocking metric.

  **Day-9 accuracy delta** (full 50-fixture harness, Claude Haiku 4.5, ~\$0.13 cost per run):
  - **LLM mode accuracy (Claude Haiku 4.5) — measured non-deterministic.** Two independent harness runs on the same SHA returned **97.6%** and **93.5%** overall (Article 5 prohibition: 100.0% both runs; binary high-risk: 98.0% both runs). Haiku is probabilistic; neither run is the "baseline" — the range is the headline. Deterministic-mode baseline is **98.2% / 100.0% / 98.0%** (byte-stable across runs). For reproducible classification, use deterministic mode.
  - Per-bucket LLM mode (best of two runs): annex_iii 15/17, article_5 6/7, article_50 7/8, negative 9/9, legacy 9/9.

  **LLM-mode non-determinism note.** Unlike deterministic mode (byte-stable across runs), LLM mode results can vary slightly between runs because Haiku is a probabilistic model. Across two independent harness runs during the Day-9 build we observed overall accuracy fluctuating between 93.5% and 97.6%. For reproducible classification, prefer deterministic mode. The deterministic mode is generally more reliable on the curated corpus because the corpus was shaped to match the lexicon's canonical phrases. LLM mode trades reproducibility for better coverage of semantically-similar paraphrases that don't appear in the lexicon. Reports: `accuracy/REPORT.md` (deterministic) and `accuracy/REPORT.llm-anthropic.md` (LLM mode).

  **Tests** — 28 new unit tests across `test/extract/llm.spec.ts` (9, dispatch validation + Day-10 deferral + anthropic routing), `test/extract/providers/anthropic.spec.ts` (19, mocked SDK happy paths + hallucination filtering + error paths + protocol details + API-key redaction). 5 new CLI tests in `test/cli.spec.ts` (--llm anthropic without env → exit 3, --llm openai/groq → exit 3 Day-10 deferral, --llm mistral → exit 3 unknown-provider, --help mentions --llm). 5 new accuracy-harness tests in `test/accuracy/accuracy-llm.spec.ts` (offline mocked-SDK 50-fixture run + bounded concurrency + structural invariants). 3 gated integration tests in `test/extract/llm-anthropic-integration.spec.ts` (real-API; runs only when LUCAIRN_LLM_INTEGRATION=1 + ANTHROPIC_API_KEY set; cost \$0.005 per run; verified end-to-end during dispatch). Test count 425+1 = 426 → 463+4 = 467 (+38 new tests, of which 3 skip without the env vars).

  **Stable error-code prefixes** (CLI exits 3 on any of these): `LLM_NO_API_KEY:`, `LLM_SDK_NOT_INSTALLED:`, `LLM_UNKNOWN_PROVIDER:`, `LLM_PROVIDER_NOT_IMPLEMENTED:`, `LLM_NO_TOOL_USE:`, `LLM_PARSE_ERROR:`, `LLM_API_ERROR:`. The API key is redacted from any wrapped error message.

  **Day-8 carry-forward closures bundled in:**
  - REPORT.md `Rules version: v0.1.0` → `v0.1.1` (citation-verification step suggested-fix from PR #8).
  - review-pass NIT-2 (Day-8): `narrowSubLetters()` ¶7 polygraph branch comment now mentions Day-8 G-3 plurals.
  - citation-verification step WARN A (¶3(c) DE paraphrase): added `bewertung des bildungsniveaus` + `bewertung angemessenes bildungsniveau` to DE lexicon's `annex_iii.3_education.entries`; extended ¶3(c) narrowing branch.
  - citation-verification step WARN B (¶7(b) DE paraphrase): added `risiko der irregulären einwanderung` + `irreguläre einwanderung` to DE lexicon's `annex_iii.7_migration_border.entries`; extended ¶7(b) narrowing branch.
  - citation-verification step WARN C (Art 5(1)(d) DE colloquial paraphrase): kept as-is (defensible loosening per Day-8 decision); rule-module header comment documents the architecture.

  EUR-Lex grounding unchanged: Regulation (EU) 2024/1689 Annex III + Article 5 + Article 50.

- **Day 8 — lexicon patch, G-1/G-2/G-3/G-4 closure, real-accuracy measurement.** Closes the 4 highest-priority Day-7 polish items (G-1 Annex III sub-letter narrowing for ¶¶ 2/3/7/8; G-2 ¶6(a) victim-risk coverage; G-3 plurals + DE morphology via additive lexicon variants; G-4(a) Art 5(1)(d) disambiguator paraphrase coverage + G-4(b) natural-German variants alongside lexicon-canonical phrases). Rewrites 5 DE Day-7 contamination fixtures (19/21/23/28/30) with natural German per EU/DE consultant judgment; the credibility moat is now consistent ("every fixture reads as natural German"). Backfills `expected.annex_iii_sub_letters` on 8 Day-7 fixtures and `bucket` + `source_url` + `article_50_paragraphs` on the 2 day5 fixtures (M-3). Path A chosen for fixture-12 (combine recidivism + victim-risk in one sentence using `sowie` connector); corpus size stays at 50. New G-5 entry filed: fixture-28's natural-German rewrite surfaced a compound-noun tokenization gap (`emotionserkennung` 1-gram doesn't fire inside `Emotionserkennungssystems`) — shipped honestly rather than engineered away. Adds `CI_OVERALL_FLOOR_V1_LAUNCH = 0.85` informational constant in `scripts/accuracy.ts` and a `.skip()` vitest spec asserting the v1.0 launch target; flipping to `.it()` becomes a 1-character ratchet for Day 13. METHODOLOGY.md §"CI floor vs v1.0 release targets" updated with binary high-risk row (≥85% sanity floor; ≥90% v1.0 target) and §3 "Sub-letter narrowing is incomplete" rewritten to reflect completeness across all 8 Annex III paragraphs. KNOWN-MISCLASSIFICATIONS.md G-1/G-2/G-3/G-4/M-3 marked closed with commit SHAs; G-5 added under high-priority gaps.

  **Final numbers** (pnpm accuracy on 50 fixtures, v0.1.1 rule-set):
  - Overall accuracy: **98.2%** (was 100.0% pre-rewrite — honest disclosure of the 1.8% residual).
  - Article 5 prohibition: **100.0%** (safety-critical zero-false-negative bar unchanged).
  - Binary high-risk: **98.0%** (was 100.0%).

  **Files touched** (across 10 atomic commits):
  - `src/rules/article-6-annex-iii.ts` — `narrowSubLetters()` extended with branches for paragraphs 2/3/7/8 + ¶6(a) victim-risk clause.
  - `src/rules/article-5.ts` — predictive-policing disambiguator substring sets extended with 5 EN + 5 DE paraphrase variants.
  - `src/data/patterns.{en,de}.json` — additive lexicon entries only (~58 new entries total across G-1/G-2/G-3/G-5 expansions). No existing entry removed.
  - `test/fixtures/use-cases/day7/{19,21,23,28,30}-*.json` — 5 contamination fixtures rewritten with natural German.
  - `test/fixtures/use-cases/day7/{04,05,06,07,14,15,16,17}-*.json` — 8 fixtures backfilled with `annex_iii_sub_letters`.
  - `test/fixtures/use-cases/day7/12-*.json` — Path A combined victim-risk + recidivism.
  - `test/fixtures/use-cases/day5/{01,02}-*.json` — M-3 backfill (`bucket`, `source_url`, `article_50_paragraphs`).
  - `test/rules/article-{5,6-annex-iii}.spec.ts` — 18 new tests (11 for narrowing, 2 for victim-risk, 4 for Art 5(1)(d) paraphrases, 1 invariant).
  - `test/accuracy/accuracy.spec.ts` — per-bucket counts updated for the day5 bucket shift; new `.skip()` spec for v1.0 launch target.
  - `scripts/accuracy.ts` — `CI_OVERALL_FLOOR_V1_LAUNCH = 0.85` informational constant.
  - `test/classify/__snapshots__/snapshots.spec.ts.snap` + 4 other snap files — rules_hash rolled `3b3e3d37` → `4a8a49ba`; sub_letters non-empty for day3 ¶2/¶3/¶7/¶8 fixtures.
  - `accuracy/REPORT.md` — regenerated; per-bucket totals reflect new article_50 (8) + legacy (9) split.
  - `accuracy/METHODOLOGY.md` — §3 "Sub-letter narrowing" updated to reflect completeness; §9 + §10 added documenting Day-8 fixture rewrites + the G-5 residual.
  - `accuracy/KNOWN-MISCLASSIFICATIONS.md` — G-1/G-2/G-3/G-4 marked closed with commit SHAs; G-5 entry filed under high-priority gaps; M-3 marked closed.
  - `README.md` + `README.de.md` — Accuracy section updated to v0.1.1 numbers and Day-8 disclosure paragraph.

  Test count: 406 → 424 (15 new in article-6-annex-iii.spec, 4 new in article-5.spec; net +18). All gates green: `pnpm typecheck`, `pnpm typecheck:test`, `pnpm test`, `pnpm accuracy:check`, `pnpm sync:three-category` (no drift), `pnpm build`.

  EUR-Lex grounding unchanged: Regulation (EU) 2024/1689 Annex III paragraphs 1-8 + Article 5(1) letters a-h + Article 50 paragraphs 1-5. Lexicon expansion remains MIT-licensed; fixture rewrites remain CC-BY-4.0.

- **Day 7 — 50-case bilingual fixture corpus + accuracy harness + CI floor + paragraph projection helper.** Adds 39 new bilingual use-case fixtures at `test/fixtures/use-cases/day7/` to complete the locked 50-case corpus matrix (17 Annex III high-risk + 7 Article 5 prohibited + 6 Article 50 transparency + 9 negative cases). Combined with the 11 existing day3/4/5 fixtures: 24 Annex III + 8 Article 5 + 8 Article 50 + 10 negatives; 21 EN + 29 DE. The 58:42 DE:EN split intentionally favours German credibility for the EU/DE consultant launch audience; every DE fixture's INPUT text is hand-written German-natural (BSI / BfDI / Bitkom / EUR-Lex DE PDF vocabulary), not Google-Translated EN. Fixture-schema additively extends the legacy `Fixture` interface with optional fields: `article_50_paragraphs` (sorted paragraph-id array), `three_category_applicable`, `annex_iv_required`, `article_{10,12,13,14,15}_applicable`, plus top-level `bucket` ('annex_iii' | 'article_5' | 'article_50' | 'negative') and `source_url` (allowlisted Tier-1 regulator hosts). Absent fields are SKIPPED by the harness — the additive-schema guarantee that lets legacy fixtures coexist without backfilling.

  **New `scripts/accuracy.ts` harness** — pure-TypeScript script invoked via `pnpm accuracy`. Loads every JSON fixture under `test/fixtures/use-cases/day{3,4,5,7}/`, runs each through `classify(fixture.input, { lang: fixture.lang })`, compares actual against expected on every present field, computes three headline metrics (overall accuracy = granular per-field pass rate; Article 5 accuracy = safety-critical zero-false-negative bar across all 50 fixtures; binary high-risk accuracy across all 50 fixtures), per-bucket pass-all accuracy, and emits `accuracy/REPORT.md` (committed) + `accuracy/REPORT.json` (gitignored). CI gate: exit 0 only if `overall_accuracy >= 0.80 AND article_5_accuracy === 1.0`; exit 1 otherwise. Deterministic — same fixtures + same rules → byte-stable report bytes (modulo `last_run_at` timestamp; supply via `LAST_RUN_AT_OVERRIDE` env for stable rebuilds). CLI flags: `--verbose` (per-fixture pass/fail to stdout), `--format json|markdown|both` (default both).

  **New vitest spec** at `test/accuracy/accuracy.spec.ts` that imports `runAccuracy()` directly, runs it, and asserts on the same CI floor inside `pnpm test`. Asserts structural invariants too: 50 fixtures loaded; per-bucket counts (17/7/6/9 + 11 legacy = 50); misclassifications list aligns with fixture pass/fail; every fixture emits ≥1 field_check (no silent skip); rules_version + rules_hash + overall_accuracy stable across two consecutive harness runs (purity).

  **New `src/util/article-50-paragraphs.ts`** — pure-function helper `projectArticle50Paragraphs(result)` projecting `Article50Result.triggered_by` into a sorted, deduplicated `('50(1)' | '50(2)' | '50(3)' | '50(4)_sub1' | '50(4)_sub2')[]` array. Bridges the fixture-schema's `expected.article_50_paragraphs` string-array against the classifier's internal boolean-shape. 50(5) is INTENTIONALLY OMITTED from the projection — it's format-and-timing language that trails any 50(1)..(4) fire, not an independent fire signal. Documented in the helper file header. 15 new unit tests at `test/util/article-50-paragraphs.spec.ts` cover the type-guards (null, arrays, missing triggered_by), each of 5 paragraph triggers individually, 50(4) sub-1+sub-2 simultaneous fire, sorted-ASCII determinism, all-5-fire, explicit '50(5)' exclusion invariant, and end-to-end via `classifyArticle50()` on synthesized EN chatbot + DE deepfake inputs.

  **New `accuracy/METHODOLOGY.md` and `accuracy/KNOWN-MISCLASSIFICATIONS.md`** — public artifacts at launch. METHODOLOGY documents the coverage matrix, source allowlist, per-target metrics, "absent field = skip" semantics, CI floor vs v1.0 release targets, "Honest limitations" §1-8 (the 100% headline is internal consistency between curated fixtures and curated lexicon — not arbitrary real-world accuracy; plurals + German morphology not normalised; sub-letter narrowing only for Annex III ¶¶ 1,4,5,6; Art 5(1)(d) disambiguator is substring-not-n-gram; no LLM-extractor harness pass; no adversarial cases), reproduce-the-report runbook, and how-to-contribute. KNOWN-MISCLASSIFICATIONS enumerates v0.2 polish backlog with priority tags (G-1 narrowing extension, G-2 Annex III.6(a) victim-risk lexicon gap, G-3 stemming/lemmatization, G-4 disambiguator paraphrase coverage; M-1 worker-monitoring 2-gram, M-2 set-equality unification, M-3 legacy-fixture backfill, M-4 adversarial sub-bucket, M-5 LLM-extractor harness; L-1/L-2/L-3 low-priority).

  **New `accuracy/REPORT.md`** — initial v0.1.0 report committed for traceability. Run: 50 fixtures, overall 100.0%, Art 5 100.0%, binary high-risk 100.0%; per-bucket annex_iii 17/17, article_5 7/7, article_50 6/6, negative 9/9, legacy 11/11. The 100% reflects the fixture-engineering loop (fixtures were shaped during Day-7 to match the lexicon's canonical phrases) and is documented as such in METHODOLOGY §"Honest limitations".

  **New `pnpm` scripts** `accuracy` (CLI run) and `accuracy:check` (vitest-spec-only). `.gitignore` extended for `accuracy/REPORT.json` (machine-readable artifact; only the .md is committed). README.md and README.de.md carry the new `## Accuracy` / `## Genauigkeit (Accuracy)` section linking to METHODOLOGY + KNOWN-MISCLASSIFICATIONS.

  **Day-2 lexicon coverage gaps surfaced + documented** (NOT patched in this PR per dispatch §1.9). The harness's first run found that several Day-7 fixtures couldn't trigger the expected classification because (a) `narrowSubLetters()` at `src/rules/article-6-annex-iii.ts:285-468` only implements sub-letter narrowing for Annex III ¶¶ 1, 4, 5, 6 (¶¶ 2, 3, 7, 8 emit `sub_letters: []`); (b) plurals and German morphology aren't normalised (`asylum applications` ≠ `asylum application`; `Visumantragen` ≠ `Visumantrag`; `Rückfallrisikos` ≠ `Rückfallrisiko`); (c) the Art 5(1)(d) "solely on profiling" disambiguator is `String.prototype.includes` substring-match, not n-gram-match, so paraphrased forms don't trigger. Per dispatch §1.9 each gap is documented in `accuracy/KNOWN-MISCLASSIFICATIONS.md` as a v0.2 polish backlog item; Day-7 fixtures were shaped to use the lexicon's canonical phrases.

  Test count 382 → 406 (15 helper tests + 9 accuracy-harness invariants). Total commits in PR: 6 atomic (Bucket A 16; Bucket B 7; Bucket C 6 + helper + spec; Bucket D 9; harness + alignment; docs). EUR-Lex grounding: Regulation (EU) 2024/1689 Annex III paragraphs 1-8 + Article 5(1) letters a-h + Article 50 paragraphs 1-5.

- **Day 6 — CLI surface + formatters + citations.json + i18n locales + rules-hash util + Annex IV reference.** Replaces the Day-1 stub `src/cli.ts` (3-line scaffold print) with a real `commander`-based entrypoint and the Day-1 stub `src/classify.ts` placeholder shape with a real structured-evidence `ClassifyResult` that carries each rule module's native result (`article_5: Article5Result`, `annex_iii: AnnexIIIResult`, `article_10..15: Article{N}Result`, `article_50: Article50Result`, `three_category: ThreeCategoryResult | null`). `ClassifyOptions` accepts `lang`, `llm` (reserved for Day 9), `threeCategory`, `rulesVersion`. Orchestration order locked: extractFeatures → classifyArticle5 → classifyAnnexIII → classifyArticle10/12/13/14/15 → classifyArticle50 → classifyThreeCategory (when not opted out) → annex_iv_required derivation → confidence (v0.1 placeholder formula, Day 8 will tune). Three new public-facing surfaces: (a) `src/format/cli-table.ts` — multi-line CLI render matching the plan's example output (line 52-76) with Article 12 included between Article 10 and Article 13 (plan example pre-dates Day 5); color via `kleur` with a Proxy-based no-op kleur replacement when useColor is false (handles both `k.red(text)` and chained `k.red().bold(text)` — the apply trap intercepts no-arg calls so the underlying identity fn never receives undefined and triggers `String.prototype.bold` HTML wrapper, a real bug observed during local smoke before the apply-trap fix); `formatAnnexIVReference()` exported for the `--annex iv` path (locale-keyed verbatim Annex IV titles from i18n). (b) `src/format/json.ts` — stable-key-order serialization (top-level keys locked in `KEY_ORDER`); `features` OMITTED by default (consultants don't need every lexicon hit), enabled via `--json` + `AI_ACT_CLASSIFY_INCLUDE_FEATURES=1` env. (c) `src/format/markdown.ts` — GitHub-flavoured markdown with emoji status pills (✅/⚠️/🛑/📄/⬜/🔴), tables for article mapping + category overlay, optional Citations section, mandatory disclaimer footer. CLI flags: `--format cli|json|markdown` (default cli), `--json` (shortcut for --format json), `--lang en|de` (override; default auto-detect from LANG/LC_ALL env, fallback to en), `--cite` (emit citation block), `--no-three-category` (suppress Lucairn overlay), `--rules-version <v>` (verify; exit 2 on mismatch), `--annex iv` (print static Annex IV reference and exit 0; `--annex <anything-else>` exits 2). stdin pipe support: when no positional text and process.stdin is non-TTY, read stdin to EOF. Exit codes match plan canonical (line 89): 0 = ok, 1 = Article 5 prohibited triggered, 2 = parse error (empty input, invalid flag, version mismatch, `--annex` without `iv`), 3 = LLM error (reserved for Day 9; Day-6 code never emits it). Color discipline: `process.stdout.isTTY === true && !NO_COLOR && !AI_ACT_CLASSIFY_NO_COLOR`. `AI_ACT_CLASSIFY_DEBUG=1` env re-enables stack traces on unexpected throws.

  **New regulator-citation database** (`src/data/citations.json`) keyed by article ID (article_5, annex_iii, article_10, article_12, article_13, article_14, article_15, article_50, annex_iv). Each entry carries EUR-Lex HTML + PDF URLs (EN+DE) at the regulation level (no per-article anchors per the Day-5 citation-verification step finding that EUR-Lex anchors are inconsistent across page versions), per-article EU AI Office Service Desk URLs from artificialintelligenceact.eu (Tier-2), and Lucairn commentary URLs from lucairn.eu/{en,de}/blog/eu-ai-act-architecture-2026 (Tier-3, commentary only). All 9 DE Service Desk URLs verified live 2026-05-15 (PAGE_EXISTS via WebFetch). citations.json is INTENTIONALLY EXCLUDED from the rules-hash set per dispatch spec locked decision #7 — citations are documentation, not classification rules; changing a citation URL should not bump the hash.

  **New i18n locale bundles** (`src/i18n/{en,de}.json`) with locale-keyed labels for CLI table rendering (section headings, status pills, confidence/mode/rules line, citation labels, error messages, disclaimer footer text) plus an `annex_iv_reference` array carrying the 9 numbered top-level Annex IV requirements verbatim from Tier-1 EUR-Lex Regulation (EU) 2024/1689, Annex IV / Anhang IV (PDF) — EN+DE both Tier-1 sourced. The 9 top-level requirements are truncated to title-only form (period replaces the trailing comma/colon plus enumerated sub-letters) so the CLI prints a stable 9-line reference table; this is the only deliberate divergence from Tier-1. PR #6 fix-up commit replaced the initial Tier-3 DE wording (artificialintelligenceact.eu/de/annex/4/ — which paraphrased Tier-1 with "Eine "-prefixed openings) with verbatim Tier-1 DE; the EN side already came from Tier-1. Loader (`src/i18n/load.ts`) reads at module init (mirrors the readFileSync + fileURLToPath pattern used elsewhere).

  **New rules-hash utility** (`src/util/rules-hash.ts`) — `RULES_VERSION` (sourced from `package.json` `version` field with `v` prefix; collapses into rules-vN.json later per plan line 120 but for Day 6 the collapse hasn't happened), `RULES_HASH` (first 8 hex chars of SHA-256 over `annex-iii.json + patterns.de.json + patterns.en.json + three-category.gen.json` sorted by filename, separator `\\0` between filename and bytes and between successive files), `RULES_HASH_FULL_HEX` (full 64-char hex digest, surfaced on `ClassifyResult.rules_hash_full`). All computed once at module init.

  **Build script extended** to copy `src/i18n/{en,de}.json → dist/i18n/` (the `cp -R src/i18n dist/i18n` form nested under the existing `dist/i18n/` directory tsc creates for `load.ts`, so the build copies JSON files explicitly). New runtime deps: `commander ^12.1.0`, `kleur ^4.1.5`, `cli-table3 ^0.6.5` (the latter reserved for future per-domain table rendering; cli-table.ts uses straight string concatenation because the plan's example output is not a grid). All three packages are zero-network, MIT licensed.

  **Test coverage** — 116 new tests across `test/util/rules-hash.spec.ts` (8 — deterministic hash assertions + independent recomputation + reload helper invariants), `test/classify.spec.ts` REWRITTEN from the Day-1 stub (49 — input validation + result-shape coverage + opts.threeCategory + opts.lang override + opts.rulesVersion + 11-fixture invariants + confidence formula sanity), `test/classify/snapshots.spec.ts` (14 — end-to-end orchestration snapshots across all 11 Day-3/4/5 fixtures, projected to load-bearing fields only per the Day-3 lesson-2 design choice), `test/format/cli-table.spec.ts` (24 — high-risk EN+DE snapshots × cite × --no-three-category matrix, color discipline including force-enabled ANSI invariant added in PR #6 fix-up, disclaimer-in-every-variant invariant, `formatAnnexIVReference` + `formatAnnexIVReferenceJson` + `formatAnnexIVReferenceMarkdown` EN+DE coverage), `test/format/json.spec.ts` (10 — type-guards + key-order invariant + pretty-vs-compact + features-omit-vs-include + JSON.parse round-trip), `test/format/markdown.spec.ts` (7 — type-guards + EN+DE snapshots + cite toggle + --no-three-category toggle + disclaimer present), `test/cli.spec.ts` (22 — spawnSync against `dist/cli.js`; sandbox-EPERM-friendly skip when `dist/` is absent; PR #6 fix-up added H1 + L1 + M4 coverage). Total test count 252 → 382 (368 after the Day-6 initial commit; PR #6 fix-up added 14 closing review-pass H1 + M1 + M2 + M4 + L1).

### Fixed

- Day 6 PR #6 fix-up: closed reviewer-chain citation-verification step 3 FAIL (Tier-1 EUR-Lex DE verbatim restoration for Annex IV / Anhang IV titles in `src/i18n/de.json`; Article 5 + Article 13 DE labels in `src/data/citations.json`); review-pass HIGH (`--annex iv` now respects `--rules-version`); 4 actionable MEDIUM (DE `Annex III` literal → locale-aware `Anhang III` prefix in cli-table + markdown formatters via new `annex_iii_prefix` i18n label; build script `cp src/i18n/*.json` glob form so new locales auto-ship; `--annex iv` now honours `--format json` / `--format markdown` / `--json` via new `formatAnnexIVReferenceJson()` + `formatAnnexIVReferenceMarkdown()` exports; force-enabled kleur ANSI-invariant test); 2 LOW (commander unknown-flag now exits 2 via `exitOverride()` mapping `CommanderError` codes; `chmod +x dist/cli.js` moved from `prepack` to `build` so a fresh `pnpm build` produces a directly-runnable binary); + 2 personal-info-leak scan WARN (stripped internal-build-plan filename references from `src/cli.ts` + `src/format/cli-table.ts` code comments).
- Day 3 fix-up: closed reviewer-chain HIGH × 2 (Annex III.5 multi-sub-letter accumulation; DE Art 5(1)(d) disambiguator preposition-order coverage) + 1 MEDIUM (annex-iii.json _meta.notice truth-in-claim) + 1 WARN (paragraph-3 title_de matches Tier-3 DE mirrors). Deferred to v0.2 polish: paragraph-4 / paragraph-7 title_de pending Tier-1 EUR-Lex DE PDF confirm, and 4 reasoning-trace polish items (insurance mixed-line, raw-input normalization, 6(d)/6(e) default disambiguation, narrowSubLetters / annex-iii reload coupling).

### Added

- **Day 5 — Article 12 + Article 50 rule modules + scripts/sync-three-category.ts + Lucairn three-category overlay.** Scope-decision note: plan line 365 enumerated 8 rule modules (article-5, 6+annex-iii, 10, 13, 14, 15, 50, three-category); Day 5 ships 9 module files by promoting Article 12 to a standalone module (`src/rules/article-12.ts`) instead of inlining it inside `three-category.ts`. Every other Article in scope (5, 6+annex-iii, 10, 13, 14, 15) has its own module file; inlining Art 12 would have broken the per-Article-one-module discipline, and the locked three-category mapping (Cat 2 = Art 12+14, Cat 3 = Art 10+12+14+15) needs an explicit emit-and-test surface so citation-verification step can independently cite Article 12. Five new deliverables: (a) `src/rules/article-12.ts` (record-keeping cascade) mirroring the Day-4 high-risk-cascade shape (consumes `AnnexIIIResult + Article5Result`, emits `Article12Result` with `applicable`, `triggered_by`, EUR-Lex-cited summaries, source URL); (b) `src/rules/article-50.ts` (GPAI / deployer transparency) as a NEW non-cascade root NOT gated on Annex III high-risk and NOT suppressed by Article 5 (per 50(6) "without prejudice to other transparency obligations") — classifies FIVE paragraph paths independently (50(1) chatbot interaction, 50(2) synthetic content, 50(3) emotion-recognition or biometric-categorisation deployment, 50(4) sub-paragraph 1 deep fake content, 50(4) sub-paragraph 2 public-interest text), consuming the Day-2 lexicon's `article_50_gpai` group (Day-2 lexicon extended in this commit with a new `5_public_interest_text` category covering EUR-Lex Art 50(4) second sub-paragraph). Optional `annex` parameter wires a 50(3) fallback when Annex III paragraph 1 fires with sub-letter `b` (biometric categorisation) or `c` (emotion recognition) so Annex-III-style descriptions still surface the transparency obligation; (c) `scripts/sync-three-category.ts` build-time sync script with `--check` drift-detection mode, that reads the website's compliance checklist source-of-truth at `theveil-website/src/lib/compliance/checklist-content.ts` and emits `src/data/three-category.gen.json` (committed for reproducibility — the build script's `cp -R src/data dist/data` step ships the generated JSON in the npm tarball). CI gate: `pnpm sync:three-category:check` exits 1 on drift. (d) `src/rules/three-category.ts` Lucairn opinionated overlay projecting Day-4 cascade results (Art 10 / 12 / 14 / 15) into Cat 1 (Art 10+15 sanitizer) / Cat 2 (Art 12+14 evidence) / Cat 3 (Art 10+12+14+15 inventory) — strict-AND applicability: a category is `applicable: true` iff ALL its required articles are applicable. `triggered_articles` surfaces the partial-applicable subset for transparency. Article 13 and Article 50 are INTENTIONALLY OUT of any three-category pairing (Art 13 is transparency-to-deployer track; Art 50 has a different cascade root). Article 5 suppression propagates through Day-4 cascade modules to three-category automatically (no explicit article5 branch inside the overlay); a sanity guard throws on inconsistent upstream state (article5.prohibited true but annex.suppressed_by_article_5 false). `source` field is an OBJECT `{generated_file, source_file, version}` providing provenance metadata (NOT EUR-Lex — regulatory citations live on per-article modules). EUR-Lex EN+DE wording sourced from Tier-1 EUR-Lex PDF (page 59 for Art 12; pages 82-83 for Art 50). Tier-2 EU AI Office Service Desk (`/en/ai-act/article-{12,50}` + `/de/ai-act/article-{12,50}`) was used as cross-confirmation. Tier-1 EUR-Lex HTML returned empty on programmatic fetch as of 2026-05-15; the PDF fetch succeeded with the full wording. **Post-fix-up provenance** (correcting the initial Day-5 commit): Article 12 SUMMARY_DE is verbatim Tier-1 EUR-Lex DE PDF (NOT Tier-3 mirror as initially claimed — the Tier-3 mirror's wording diverges). Article 50 EN chapeaux are verbatim Tier-1 EUR-Lex EN PDF. Article 50 DE chapeaux (all 6: 50(1), 50(2), 50(3), 50(4) sub-paragraphs 1+2, 50(5)) were replaced in the fix-up with verbatim Tier-1 EUR-Lex DE PDF — the initial commit shipped the Tier-3 mirror paraphrase, which had multiple substantive divergences from Tier-1 including 50(2) dropping the full second sentence on technical effectiveness/interoperability. Day-5 fix-up restored the missing sentence and the verbatim Tier-1 DE wording across all 6 paragraphs.

**Lexicon disclosure (Day-5 fix-up)**: the new `5_public_interest_text` category in `patterns.{en,de}.json` includes phrases `automated journalism` / `newsroom ai` (and DE equivalents) that may over-trigger on internal-team workflows. Art 50(4) sub-paragraph 2 narrows to text "published with the purpose of informing the public on matters of public interest" — internal newsroom summarization workflows that do not result in publication would not qualify. v0.2 polish target: tighten patterns to require a publication-signal phrase, or surface the false-positive risk via a Day-7 fixture and downstream consultant-judgment guidance. Article 50 summary fields enumerate all four paragraphs' statutory carve-outs verbatim (anti-hand-wave discipline per Day-3 lesson 5: 50(1)+(2)+(3)+(4) all have law-enforcement carve-outs; 50(2) also carries assistive-function carve-out; 50(4) sub-paragraph 1 also carries artistic/satirical carve-out; 50(4) sub-paragraph 2 also carries human-review/editorial-responsibility carve-out). Snapshot test extended to project `article_12`, `article_50`, and `three_category` aggregates (load-bearing fields only — `applicable`, `triggered_by` / `triggered_articles`, `required_articles`, `applicable_categories`, `source` — NOT `items[]`/`title_*`/`disclaimer_*` which are synced website-managed copy per the Day-3 lesson-2 design choice). 86 new unit tests across `test/rules/article-12.spec.ts` (14, +1 Array.isArray closure test), `test/rules/article-50.spec.ts` (36, +1 byCategory:[] guard test), `test/rules/three-category.spec.ts` (18, +1 article5:[] guard test), `test/scripts/sync-three-category.spec.ts` (10, including check-mode + drift-detection tests + fixture-based AST parsing + missing-file error path); plus 2 new Day-5 fixtures (`test/fixtures/use-cases/day5/01-art50-chatbot-en.json` + `02-art50-deepfake-de.json`) exercising the Article-50-only path (low-risk system + Art 50 transparency fires, all Day-3/4 cascades return applicable=false, three-category `applicable_categories === []`); 5 new snapshot assertions across Day-5 fixture loop; 11 regenerated snapshots covering the 9 Day-3/4 + 2 Day-5 fixtures. Total test count 165 → 251 (initial Day-5 commit landed at 247; fix-up #2 added 3 propagated-guard closure tests; fix-up #3 added 1 missing-file error-path test). New devDependency: `tsx@^4.22.0` (TypeScript runner for the sync script). New `package.json` scripts: `"sync:three-category"` and `"sync:three-category:check"`.
- **Day 4 — Article 10/13/14/15 high-risk obligation cascade rule modules.** Four pure-function classifiers that cascade off `AnnexIIIResult.high_risk && !suppressed_by_article_5` and emit `Article{N}Result` with `applicable`, `triggered_by` (article_5 flag + annex_iii_domains list), `summary_en`/`summary_de` (verbatim EUR-Lex Article 10/13/14/15 chapeaux), and EUR-Lex `source`. Article 10 applies to all high-risk systems per EUR-Lex Art 10(1) chapeau ("which make use of techniques involving the training of AI models with data") — does NOT narrow to specific Annex III paragraphs, contra the Day-3 handover's hand-wave "applies to Annex III 1+5+6+7" interpretation. Article 12 (record-keeping/logging) is intentionally NOT a Day-4 module — it shows up in `three-category.ts` Day 5 as Cat-2 + Cat-3 pairing. All four modules consume `(AnnexIIIResult, Article5Result)` and never duplicate the high-risk decision; they project the upstream signal into an applicability + EUR-Lex-cited summary. EUR-Lex EN+DE wording sourced via Tier-2 EU AI Office Service Desk (`/en/ai-act/article-{10,13,14,15}` + `/de/ai-act/article-{10,13,14,15}`) — Tier-1 EUR-Lex HTML returned empty on programmatic fetch as of 2026-05-15 (same JS-shell rendering issue seen Day 3); Tier-1 PDF remains the canonical primary source. No Tier-3 cross-validation needed (Tier-2 DE coverage complete for these four articles). 1 new Day-4 fixture (`test/fixtures/use-cases/day4/01-low-risk-en.json`) exercises the non-high-risk path (low-risk office-cleaning-scheduler — distinct from the 8 Day-3 fixtures which cover prohibition + 7 high-risk paths). Snapshot test extended to project Article-10/13/14/15 results across all 8 Day-3 + 1 Day-4 fixtures (load-bearing fields only — `applicable`, `triggered_by`, `source` — per the Day-3 lesson-2 design choice). 52 new unit tests across `test/rules/article-{10,13,14,15}.spec.ts` (13 each) + 5 new snapshot-spec assertions; total test count 108 → 165.
- **Day 3 — Article 5 prohibited-practices + Article 6 / Annex III high-risk classification rule modules with EN+DE disambiguation, scope-qualifier handling, and 8-fixture snapshot test suite.** New `src/rules/article-5.ts` (8 letters a-h with the Art 5(1)(d) "solely on profiling" disambiguator), `src/rules/article-6-annex-iii.ts` (8 high-risk domains with sub-letter narrowing for domains 1, 4, 5, 6 and Annex III.5(c) life/health insurance scope rule), `src/rules/index.ts` barrel, and `src/data/annex-iii.json` (EUR-Lex-cited Annex III data for all 8 domains with EN+DE summaries). Article 5 prohibition supersedes Annex III high-risk in `classifyAnnexIII()`. `internal_use` scope qualifier never suppresses; `research_only` Art 2(8) carve-out is blocked when "real-world conditions" / "Realbedingungen" phrasing is present. 8 bilingual test fixtures at `test/fixtures/use-cases/day3/` (5 EN + 3 DE), 32 unit tests in `test/rules/article-5.spec.ts` + `test/rules/article-6-annex-iii.spec.ts`, plus 16 snapshot assertions in `test/rules/snapshots.spec.ts`.
- **Day 2 — keyword extractor + EN+DE seed lexicons:** deterministic feature extractor (`src/extract/normalize.ts`, `lang.ts`, `keyword.ts`) with NFKC normalization, EN+DE punctuation handling, hyphen-split for German compound nouns, runtime-discovered lexicon groups for v0.2+ extensibility. EN + DE seed lexicons (`src/data/patterns.{en,de}.json`) covering 8 Annex III categories + 8 Article 5 prohibited categories + 4 Article 50 transparency categories + 3 scope qualifiers (~180 phrases total) with `_meta.notice` flagging the citation-verification step verification gate. 43 unit tests across `test/extract/`. Packaging hardened: `build` script copies `src/data/` into `dist/` so the published npm tarball ships the lexicons.
- **Day 1 scaffold:** repo + `package.json` + `tsconfig.json` (Lucairn @lucairn/* TypeScript conventions), MIT license + CC-BY-4.0 dataset license, vitest config, GitHub Actions CI matrix (Node 18/20/22 × Ubuntu/macOS), README EN + DE with disclaimer + scope blocks, `src/cli.ts` + `src/classify.ts` + `src/index.ts` stubs, `test/classify.spec.ts` smoke covering empty-input and not-yet-implemented guards.

## [0.1.0] — planned

Initial public release. Will include: rules engine for Art 5 / 6+Annex III / 10 / 13 / 14 / 15 / 50, three-category overlay synced from the Lucairn website's compliance checklist source-of-truth, 50-case bilingual fixture dataset, CLI surface, hosted UI mirror at `lucairn.eu/tools/ai-act-classifier`. Release date will be backfilled at publish time.
