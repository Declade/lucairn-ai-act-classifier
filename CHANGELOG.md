# Changelog

All notable changes to `@lucairn/ai-act-classifier` will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- **Day 7 — 50-case bilingual fixture corpus + accuracy harness + CI floor + paragraph projection helper.** Adds 39 new bilingual use-case fixtures at `test/fixtures/use-cases/day7/` to complete the locked 50-case corpus matrix (17 Annex III high-risk + 7 Article 5 prohibited + 6 Article 50 transparency + 9 negative cases). Combined with the 11 existing day3/4/5 fixtures: 24 Annex III + 8 Article 5 + 8 Article 50 + 10 negatives; 21 EN + 29 DE. The 58:42 DE:EN split intentionally favours German credibility for the EU/DE consultant launch audience; every DE fixture's INPUT text is hand-written German-natural (BSI / BfDI / Bitkom / EUR-Lex DE PDF vocabulary), not Google-Translated EN. Fixture-schema additively extends the legacy `Fixture` interface with optional fields: `article_50_paragraphs` (sorted paragraph-id array), `three_category_applicable`, `annex_iv_required`, `article_{10,12,13,14,15}_applicable`, plus top-level `bucket` ('annex_iii' | 'article_5' | 'article_50' | 'negative') and `source_url` (allowlisted Tier-1 regulator hosts). Absent fields are SKIPPED by the harness — the additive-schema guarantee that lets legacy fixtures coexist without backfilling.

  **New `scripts/accuracy.ts` harness** — pure-TypeScript script invoked via `pnpm accuracy`. Loads every JSON fixture under `test/fixtures/use-cases/day{3,4,5,7}/`, runs each through `classify(fixture.input, { lang: fixture.lang })`, compares actual against expected on every present field, computes three headline metrics (overall accuracy = granular per-field pass rate; Article 5 accuracy = safety-critical zero-false-negative bar across all 50 fixtures; binary high-risk accuracy across all 50 fixtures), per-bucket pass-all accuracy, and emits `accuracy/REPORT.md` (committed) + `accuracy/REPORT.json` (gitignored). CI gate: exit 0 only if `overall_accuracy >= 0.80 AND article_5_accuracy === 1.0`; exit 1 otherwise. Deterministic — same fixtures + same rules → byte-stable report bytes (modulo `last_run_at` timestamp; supply via `LAST_RUN_AT_OVERRIDE` env for stable rebuilds). CLI flags: `--verbose` (per-fixture pass/fail to stdout), `--format json|markdown|both` (default both).

  **New vitest spec** at `test/accuracy/accuracy.spec.ts` that imports `runAccuracy()` directly, runs it, and asserts on the same CI floor inside `pnpm test`. Asserts structural invariants too: 50 fixtures loaded; per-bucket counts (17/7/6/9 + 11 legacy = 50); misclassifications list aligns with fixture pass/fail; every fixture emits ≥1 field_check (no silent skip); rules_version + rules_hash + overall_accuracy stable across two consecutive harness runs (purity).

  **New `src/util/article-50-paragraphs.ts`** — pure-function helper `projectArticle50Paragraphs(result)` projecting `Article50Result.triggered_by` into a sorted, deduplicated `('50(1)' | '50(2)' | '50(3)' | '50(4)_sub1' | '50(4)_sub2')[]` array. Bridges the fixture-schema's `expected.article_50_paragraphs` string-array against the classifier's internal boolean-shape. 50(5) is INTENTIONALLY OMITTED from the projection — it's format-and-timing language that trails any 50(1)..(4) fire, not an independent fire signal. Documented in the helper file header. 15 new unit tests at `test/util/article-50-paragraphs.spec.ts` cover the type-guards (null, arrays, missing triggered_by), each of 5 paragraph triggers individually, 50(4) sub-1+sub-2 simultaneous fire, sorted-ASCII determinism, all-5-fire, explicit '50(5)' exclusion invariant, and end-to-end via `classifyArticle50()` on synthesized EN chatbot + DE deepfake inputs.

  **New `accuracy/METHODOLOGY.md` and `accuracy/KNOWN-MISCLASSIFICATIONS.md`** — public artifacts at launch. METHODOLOGY documents the coverage matrix, source allowlist, per-target metrics, "absent field = skip" semantics, CI floor vs v1.0 release targets, "Honest limitations" §1-8 (the 100% headline is internal consistency between curated fixtures and curated lexicon — not arbitrary real-world accuracy; plurals + German morphology not normalised; sub-letter narrowing only for Annex III ¶¶ 1,4,5,6; Art 5(1)(d) disambiguator is substring-not-n-gram; no LLM-extractor harness pass; no adversarial cases), reproduce-the-report runbook, and how-to-contribute. KNOWN-MISCLASSIFICATIONS enumerates v0.2 polish backlog with priority tags (G-1 narrowing extension, G-2 Annex III.6(a) victim-risk lexicon gap, G-3 stemming/lemmatization, G-4 disambiguator paraphrase coverage; M-1 worker-monitoring 2-gram, M-2 set-equality unification, M-3 legacy-fixture backfill, M-4 adversarial sub-bucket, M-5 LLM-extractor harness; L-1/L-2/L-3 low-priority).

  **New `accuracy/REPORT.md`** — initial v0.1.0 report committed for traceability. Run: 50 fixtures, overall 100.0%, Art 5 100.0%, binary high-risk 100.0%; per-bucket annex_iii 17/17, article_5 7/7, article_50 6/6, negative 9/9, legacy 11/11. The 100% reflects the fixture-engineering loop (fixtures were shaped during Day-7 to match the lexicon's canonical phrases) and is documented as such in METHODOLOGY §"Honest limitations".

  **New `pnpm` scripts** `accuracy` (CLI run) and `accuracy:check` (vitest-spec-only). `.gitignore` extended for `accuracy/REPORT.json` (machine-readable artifact; only the .md is committed). README.md and README.de.md carry the new `## Accuracy` / `## Genauigkeit (Accuracy)` section linking to METHODOLOGY + KNOWN-MISCLASSIFICATIONS.

  **Day-2 lexicon coverage gaps surfaced + documented** (NOT patched in this PR per dispatch §1.9). The harness's first run found that several Day-7 fixtures couldn't trigger the expected classification because (a) `narrowSubLetters()` at `src/rules/article-6-annex-iii.ts:285-468` only implements sub-letter narrowing for Annex III ¶¶ 1, 4, 5, 6 (¶¶ 2, 3, 7, 8 emit `sub_letters: []`); (b) plurals and German morphology aren't normalised (`asylum applications` ≠ `asylum application`; `Visumantragen` ≠ `Visumantrag`; `Rückfallrisikos` ≠ `Rückfallrisiko`); (c) the Art 5(1)(d) "solely on profiling" disambiguator is `String.prototype.includes` substring-match, not n-gram-match, so paraphrased forms don't trigger. Per dispatch §1.9 each gap is documented in `accuracy/KNOWN-MISCLASSIFICATIONS.md` as a v0.2 polish backlog item; Day-7 fixtures were shaped to use the lexicon's canonical phrases.

  Test count 382 → 406 (15 helper tests + 9 accuracy-harness invariants). Total commits in PR: 6 atomic (Bucket A 16; Bucket B 7; Bucket C 6 + helper + spec; Bucket D 9; harness + alignment; docs). EUR-Lex grounding: Regulation (EU) 2024/1689 Annex III paragraphs 1-8 + Article 5(1) letters a-h + Article 50 paragraphs 1-5.

- **Day 6 — CLI surface + formatters + citations.json + i18n locales + rules-hash util + Annex IV reference.** Replaces the Day-1 stub `src/cli.ts` (3-line scaffold print) with a real `commander`-based entrypoint and the Day-1 stub `src/classify.ts` placeholder shape with a real structured-evidence `ClassifyResult` that carries each rule module's native result (`article_5: Article5Result`, `annex_iii: AnnexIIIResult`, `article_10..15: Article{N}Result`, `article_50: Article50Result`, `three_category: ThreeCategoryResult | null`). `ClassifyOptions` accepts `lang`, `llm` (reserved for Day 9), `threeCategory`, `rulesVersion`. Orchestration order locked: extractFeatures → classifyArticle5 → classifyAnnexIII → classifyArticle10/12/13/14/15 → classifyArticle50 → classifyThreeCategory (when not opted out) → annex_iv_required derivation → confidence (v0.1 placeholder formula, Day 8 will tune). Three new public-facing surfaces: (a) `src/format/cli-table.ts` — multi-line CLI render matching the plan's example output (line 52-76) with Article 12 included between Article 10 and Article 13 (plan example pre-dates Day 5); color via `kleur` with a Proxy-based no-op kleur replacement when useColor is false (handles both `k.red(text)` and chained `k.red().bold(text)` — the apply trap intercepts no-arg calls so the underlying identity fn never receives undefined and triggers `String.prototype.bold` HTML wrapper, a real bug observed during local smoke before the apply-trap fix); `formatAnnexIVReference()` exported for the `--annex iv` path (locale-keyed verbatim Annex IV titles from i18n). (b) `src/format/json.ts` — stable-key-order serialization (top-level keys locked in `KEY_ORDER`); `features` OMITTED by default (consultants don't need every lexicon hit), enabled via `--json` + `AI_ACT_CLASSIFY_INCLUDE_FEATURES=1` env. (c) `src/format/markdown.ts` — GitHub-flavoured markdown with emoji status pills (✅/⚠️/🛑/📄/⬜/🔴), tables for article mapping + category overlay, optional Citations section, mandatory disclaimer footer. CLI flags: `--format cli|json|markdown` (default cli), `--json` (shortcut for --format json), `--lang en|de` (override; default auto-detect from LANG/LC_ALL env, fallback to en), `--cite` (emit citation block), `--no-three-category` (suppress Lucairn overlay), `--rules-version <v>` (verify; exit 2 on mismatch), `--annex iv` (print static Annex IV reference and exit 0; `--annex <anything-else>` exits 2). stdin pipe support: when no positional text and process.stdin is non-TTY, read stdin to EOF. Exit codes match plan canonical (line 89): 0 = ok, 1 = Article 5 prohibited triggered, 2 = parse error (empty input, invalid flag, version mismatch, `--annex` without `iv`), 3 = LLM error (reserved for Day 9; Day-6 code never emits it). Color discipline: `process.stdout.isTTY === true && !NO_COLOR && !AI_ACT_CLASSIFY_NO_COLOR`. `AI_ACT_CLASSIFY_DEBUG=1` env re-enables stack traces on unexpected throws.

  **New regulator-citation database** (`src/data/citations.json`) keyed by article ID (article_5, annex_iii, article_10, article_12, article_13, article_14, article_15, article_50, annex_iv). Each entry carries EUR-Lex HTML + PDF URLs (EN+DE) at the regulation level (no per-article anchors per the Day-5 regulator-validator finding that EUR-Lex anchors are inconsistent across page versions), per-article EU AI Office Service Desk URLs from artificialintelligenceact.eu (Tier-2), and Lucairn commentary URLs from lucairn.eu/{en,de}/blog/eu-ai-act-architecture-2026 (Tier-3, commentary only). All 9 DE Service Desk URLs verified live 2026-05-15 (PAGE_EXISTS via WebFetch). citations.json is INTENTIONALLY EXCLUDED from the rules-hash set per dispatch spec locked decision #7 — citations are documentation, not classification rules; changing a citation URL should not bump the hash.

  **New i18n locale bundles** (`src/i18n/{en,de}.json`) with locale-keyed labels for CLI table rendering (section headings, status pills, confidence/mode/rules line, citation labels, error messages, disclaimer footer text) plus an `annex_iv_reference` array carrying the 9 numbered top-level Annex IV requirements verbatim from Tier-1 EUR-Lex Regulation (EU) 2024/1689, Annex IV / Anhang IV (PDF) — EN+DE both Tier-1 sourced. The 9 top-level requirements are truncated to title-only form (period replaces the trailing comma/colon plus enumerated sub-letters) so the CLI prints a stable 9-line reference table; this is the only deliberate divergence from Tier-1. PR #6 fix-up commit replaced the initial Tier-3 DE wording (artificialintelligenceact.eu/de/annex/4/ — which paraphrased Tier-1 with "Eine "-prefixed openings) with verbatim Tier-1 DE; the EN side already came from Tier-1. Loader (`src/i18n/load.ts`) reads at module init (mirrors the readFileSync + fileURLToPath pattern used elsewhere).

  **New rules-hash utility** (`src/util/rules-hash.ts`) — `RULES_VERSION` (sourced from `package.json` `version` field with `v` prefix; collapses into rules-vN.json later per plan line 120 but for Day 6 the collapse hasn't happened), `RULES_HASH` (first 8 hex chars of SHA-256 over `annex-iii.json + patterns.de.json + patterns.en.json + three-category.gen.json` sorted by filename, separator `\\0` between filename and bytes and between successive files), `RULES_HASH_FULL_HEX` (full 64-char hex digest, surfaced on `ClassifyResult.rules_hash_full`). All computed once at module init.

  **Build script extended** to copy `src/i18n/{en,de}.json → dist/i18n/` (the `cp -R src/i18n dist/i18n` form nested under the existing `dist/i18n/` directory tsc creates for `load.ts`, so the build copies JSON files explicitly). New runtime deps: `commander ^12.1.0`, `kleur ^4.1.5`, `cli-table3 ^0.6.5` (the latter reserved for future per-domain table rendering; cli-table.ts uses straight string concatenation because the plan's example output is not a grid). All three packages are zero-network, MIT licensed.

  **Test coverage** — 116 new tests across `test/util/rules-hash.spec.ts` (8 — deterministic hash assertions + independent recomputation + reload helper invariants), `test/classify.spec.ts` REWRITTEN from the Day-1 stub (49 — input validation + result-shape coverage + opts.threeCategory + opts.lang override + opts.rulesVersion + 11-fixture invariants + confidence formula sanity), `test/classify/snapshots.spec.ts` (14 — end-to-end orchestration snapshots across all 11 Day-3/4/5 fixtures, projected to load-bearing fields only per the Day-3 lesson-2 design choice), `test/format/cli-table.spec.ts` (24 — high-risk EN+DE snapshots × cite × --no-three-category matrix, color discipline including force-enabled ANSI invariant added in PR #6 fix-up, disclaimer-in-every-variant invariant, `formatAnnexIVReference` + `formatAnnexIVReferenceJson` + `formatAnnexIVReferenceMarkdown` EN+DE coverage), `test/format/json.spec.ts` (10 — type-guards + key-order invariant + pretty-vs-compact + features-omit-vs-include + JSON.parse round-trip), `test/format/markdown.spec.ts` (7 — type-guards + EN+DE snapshots + cite toggle + --no-three-category toggle + disclaimer present), `test/cli.spec.ts` (22 — spawnSync against `dist/cli.js`; sandbox-EPERM-friendly skip when `dist/` is absent; PR #6 fix-up added H1 + L1 + M4 coverage). Total test count 252 → 382 (368 after the Day-6 initial commit; PR #6 fix-up added 14 closing bug-hunter H1 + M1 + M2 + M4 + L1).

### Fixed

- Day 6 PR #6 fix-up: closed reviewer-chain regulator-validator 3 FAIL (Tier-1 EUR-Lex DE verbatim restoration for Annex IV / Anhang IV titles in `src/i18n/de.json`; Article 5 + Article 13 DE labels in `src/data/citations.json`); bug-hunter HIGH (`--annex iv` now respects `--rules-version`); 4 actionable MEDIUM (DE `Annex III` literal → locale-aware `Anhang III` prefix in cli-table + markdown formatters via new `annex_iii_prefix` i18n label; build script `cp src/i18n/*.json` glob form so new locales auto-ship; `--annex iv` now honours `--format json` / `--format markdown` / `--json` via new `formatAnnexIVReferenceJson()` + `formatAnnexIVReferenceMarkdown()` exports; force-enabled kleur ANSI-invariant test); 2 LOW (commander unknown-flag now exits 2 via `exitOverride()` mapping `CommanderError` codes; `chmod +x dist/cli.js` moved from `prepack` to `build` so a fresh `pnpm build` produces a directly-runnable binary); + 2 personal-info-leak WARN (stripped internal-build-plan filename references from `src/cli.ts` + `src/format/cli-table.ts` code comments).
- Day 3 fix-up: closed reviewer-chain HIGH × 2 (Annex III.5 multi-sub-letter accumulation; DE Art 5(1)(d) disambiguator preposition-order coverage) + 1 MEDIUM (annex-iii.json _meta.notice truth-in-claim) + 1 WARN (paragraph-3 title_de matches Tier-3 DE mirrors). Deferred to v0.2 polish: paragraph-4 / paragraph-7 title_de pending Tier-1 EUR-Lex DE PDF confirm, and 4 reasoning-trace polish items (insurance mixed-line, raw-input normalization, 6(d)/6(e) default disambiguation, narrowSubLetters / annex-iii reload coupling).

### Added

- **Day 5 — Article 12 + Article 50 rule modules + scripts/sync-three-category.ts + Lucairn three-category overlay.** Scope-decision note: plan line 365 enumerated 8 rule modules (article-5, 6+annex-iii, 10, 13, 14, 15, 50, three-category); Day 5 ships 9 module files by promoting Article 12 to a standalone module (`src/rules/article-12.ts`) instead of inlining it inside `three-category.ts`. Every other Article in scope (5, 6+annex-iii, 10, 13, 14, 15) has its own module file; inlining Art 12 would have broken the per-Article-one-module discipline, and the locked three-category mapping (Cat 2 = Art 12+14, Cat 3 = Art 10+12+14+15) needs an explicit emit-and-test surface so regulator-validator can independently cite Article 12. Five new deliverables: (a) `src/rules/article-12.ts` (record-keeping cascade) mirroring the Day-4 high-risk-cascade shape (consumes `AnnexIIIResult + Article5Result`, emits `Article12Result` with `applicable`, `triggered_by`, EUR-Lex-cited summaries, source URL); (b) `src/rules/article-50.ts` (GPAI / deployer transparency) as a NEW non-cascade root NOT gated on Annex III high-risk and NOT suppressed by Article 5 (per 50(6) "without prejudice to other transparency obligations") — classifies FIVE paragraph paths independently (50(1) chatbot interaction, 50(2) synthetic content, 50(3) emotion-recognition or biometric-categorisation deployment, 50(4) sub-paragraph 1 deep fake content, 50(4) sub-paragraph 2 public-interest text), consuming the Day-2 lexicon's `article_50_gpai` group (Day-2 lexicon extended in this commit with a new `5_public_interest_text` category covering EUR-Lex Art 50(4) second sub-paragraph). Optional `annex` parameter wires a 50(3) fallback when Annex III paragraph 1 fires with sub-letter `b` (biometric categorisation) or `c` (emotion recognition) so Annex-III-style descriptions still surface the transparency obligation; (c) `scripts/sync-three-category.ts` build-time sync script with `--check` drift-detection mode, that reads the website's compliance checklist source-of-truth at `theveil-website/src/lib/compliance/checklist-content.ts` and emits `src/data/three-category.gen.json` (committed for reproducibility — the build script's `cp -R src/data dist/data` step ships the generated JSON in the npm tarball). CI gate: `pnpm sync:three-category:check` exits 1 on drift. (d) `src/rules/three-category.ts` Lucairn opinionated overlay projecting Day-4 cascade results (Art 10 / 12 / 14 / 15) into Cat 1 (Art 10+15 sanitizer) / Cat 2 (Art 12+14 evidence) / Cat 3 (Art 10+12+14+15 inventory) — strict-AND applicability: a category is `applicable: true` iff ALL its required articles are applicable. `triggered_articles` surfaces the partial-applicable subset for transparency. Article 13 and Article 50 are INTENTIONALLY OUT of any three-category pairing (Art 13 is transparency-to-deployer track; Art 50 has a different cascade root). Article 5 suppression propagates through Day-4 cascade modules to three-category automatically (no explicit article5 branch inside the overlay); a sanity guard throws on inconsistent upstream state (article5.prohibited true but annex.suppressed_by_article_5 false). `source` field is an OBJECT `{generated_file, source_file, version}` providing provenance metadata (NOT EUR-Lex — regulatory citations live on per-article modules). EUR-Lex EN+DE wording sourced from Tier-1 EUR-Lex PDF (page 59 for Art 12; pages 82-83 for Art 50). Tier-2 EU AI Office Service Desk (`/en/ai-act/article-{12,50}` + `/de/ai-act/article-{12,50}`) was used as cross-confirmation. Tier-1 EUR-Lex HTML returned empty on programmatic fetch as of 2026-05-15; the PDF fetch succeeded with the full wording. **Post-fix-up provenance** (correcting the initial Day-5 commit): Article 12 SUMMARY_DE is verbatim Tier-1 EUR-Lex DE PDF (NOT Tier-3 mirror as initially claimed — the Tier-3 mirror's wording diverges). Article 50 EN chapeaux are verbatim Tier-1 EUR-Lex EN PDF. Article 50 DE chapeaux (all 6: 50(1), 50(2), 50(3), 50(4) sub-paragraphs 1+2, 50(5)) were replaced in the fix-up with verbatim Tier-1 EUR-Lex DE PDF — the initial commit shipped the Tier-3 mirror paraphrase, which had multiple substantive divergences from Tier-1 including 50(2) dropping the full second sentence on technical effectiveness/interoperability. Day-5 fix-up restored the missing sentence and the verbatim Tier-1 DE wording across all 6 paragraphs.

**Lexicon disclosure (Day-5 fix-up)**: the new `5_public_interest_text` category in `patterns.{en,de}.json` includes phrases `automated journalism` / `newsroom ai` (and DE equivalents) that may over-trigger on internal-team workflows. Art 50(4) sub-paragraph 2 narrows to text "published with the purpose of informing the public on matters of public interest" — internal newsroom summarization workflows that do not result in publication would not qualify. v0.2 polish target: tighten patterns to require a publication-signal phrase, or surface the false-positive risk via a Day-7 fixture and downstream consultant-judgment guidance. Article 50 summary fields enumerate all four paragraphs' statutory carve-outs verbatim (anti-hand-wave discipline per Day-3 lesson 5: 50(1)+(2)+(3)+(4) all have law-enforcement carve-outs; 50(2) also carries assistive-function carve-out; 50(4) sub-paragraph 1 also carries artistic/satirical carve-out; 50(4) sub-paragraph 2 also carries human-review/editorial-responsibility carve-out). Snapshot test extended to project `article_12`, `article_50`, and `three_category` aggregates (load-bearing fields only — `applicable`, `triggered_by` / `triggered_articles`, `required_articles`, `applicable_categories`, `source` — NOT `items[]`/`title_*`/`disclaimer_*` which are synced website-managed copy per the Day-3 lesson-2 design choice). 86 new unit tests across `test/rules/article-12.spec.ts` (14, +1 Array.isArray closure test), `test/rules/article-50.spec.ts` (36, +1 byCategory:[] guard test), `test/rules/three-category.spec.ts` (18, +1 article5:[] guard test), `test/scripts/sync-three-category.spec.ts` (10, including check-mode + drift-detection tests + fixture-based AST parsing + missing-file error path); plus 2 new Day-5 fixtures (`test/fixtures/use-cases/day5/01-art50-chatbot-en.json` + `02-art50-deepfake-de.json`) exercising the Article-50-only path (low-risk system + Art 50 transparency fires, all Day-3/4 cascades return applicable=false, three-category `applicable_categories === []`); 5 new snapshot assertions across Day-5 fixture loop; 11 regenerated snapshots covering the 9 Day-3/4 + 2 Day-5 fixtures. Total test count 165 → 251 (initial Day-5 commit landed at 247; fix-up #2 added 3 propagated-guard closure tests; fix-up #3 added 1 missing-file error-path test). New devDependency: `tsx@^4.22.0` (TypeScript runner for the sync script). New `package.json` scripts: `"sync:three-category"` and `"sync:three-category:check"`.
- **Day 4 — Article 10/13/14/15 high-risk obligation cascade rule modules.** Four pure-function classifiers that cascade off `AnnexIIIResult.high_risk && !suppressed_by_article_5` and emit `Article{N}Result` with `applicable`, `triggered_by` (article_5 flag + annex_iii_domains list), `summary_en`/`summary_de` (verbatim EUR-Lex Article 10/13/14/15 chapeaux), and EUR-Lex `source`. Article 10 applies to all high-risk systems per EUR-Lex Art 10(1) chapeau ("which make use of techniques involving the training of AI models with data") — does NOT narrow to specific Annex III paragraphs, contra the Day-3 handover's hand-wave "applies to Annex III 1+5+6+7" interpretation. Article 12 (record-keeping/logging) is intentionally NOT a Day-4 module — it shows up in `three-category.ts` Day 5 as Cat-2 + Cat-3 pairing. All four modules consume `(AnnexIIIResult, Article5Result)` and never duplicate the high-risk decision; they project the upstream signal into an applicability + EUR-Lex-cited summary. EUR-Lex EN+DE wording sourced via Tier-2 EU AI Office Service Desk (`/en/ai-act/article-{10,13,14,15}` + `/de/ai-act/article-{10,13,14,15}`) — Tier-1 EUR-Lex HTML returned empty on programmatic fetch as of 2026-05-15 (same JS-shell rendering issue seen Day 3); Tier-1 PDF remains the canonical primary source. No Tier-3 cross-validation needed (Tier-2 DE coverage complete for these four articles). 1 new Day-4 fixture (`test/fixtures/use-cases/day4/01-low-risk-en.json`) exercises the non-high-risk path (low-risk office-cleaning-scheduler — distinct from the 8 Day-3 fixtures which cover prohibition + 7 high-risk paths). Snapshot test extended to project Article-10/13/14/15 results across all 8 Day-3 + 1 Day-4 fixtures (load-bearing fields only — `applicable`, `triggered_by`, `source` — per the Day-3 lesson-2 design choice). 52 new unit tests across `test/rules/article-{10,13,14,15}.spec.ts` (13 each) + 5 new snapshot-spec assertions; total test count 108 → 165.
- **Day 3 — Article 5 prohibited-practices + Article 6 / Annex III high-risk classification rule modules with EN+DE disambiguation, scope-qualifier handling, and 8-fixture snapshot test suite.** New `src/rules/article-5.ts` (8 letters a-h with the Art 5(1)(d) "solely on profiling" disambiguator), `src/rules/article-6-annex-iii.ts` (8 high-risk domains with sub-letter narrowing for domains 1, 4, 5, 6 and Annex III.5(c) life/health insurance scope rule), `src/rules/index.ts` barrel, and `src/data/annex-iii.json` (EUR-Lex-cited Annex III data for all 8 domains with EN+DE summaries). Article 5 prohibition supersedes Annex III high-risk in `classifyAnnexIII()`. `internal_use` scope qualifier never suppresses; `research_only` Art 2(8) carve-out is blocked when "real-world conditions" / "Realbedingungen" phrasing is present. 8 bilingual test fixtures at `test/fixtures/use-cases/day3/` (5 EN + 3 DE), 32 unit tests in `test/rules/article-5.spec.ts` + `test/rules/article-6-annex-iii.spec.ts`, plus 16 snapshot assertions in `test/rules/snapshots.spec.ts`.
- **Day 2 — keyword extractor + EN+DE seed lexicons:** deterministic feature extractor (`src/extract/normalize.ts`, `lang.ts`, `keyword.ts`) with NFKC normalization, EN+DE punctuation handling, hyphen-split for German compound nouns, runtime-discovered lexicon groups for v0.2+ extensibility. EN + DE seed lexicons (`src/data/patterns.{en,de}.json`) covering 8 Annex III categories + 8 Article 5 prohibited categories + 4 Article 50 transparency categories + 3 scope qualifiers (~180 phrases total) with `_meta.notice` flagging the regulator-validator verification gate. 43 unit tests across `test/extract/`. Packaging hardened: `build` script copies `src/data/` into `dist/` so the published npm tarball ships the lexicons.
- **Day 1 scaffold:** repo + `package.json` + `tsconfig.json` (Lucairn @lucairn/* TypeScript conventions), MIT license + CC-BY-4.0 dataset license, vitest config, GitHub Actions CI matrix (Node 18/20/22 × Ubuntu/macOS), README EN + DE with disclaimer + scope blocks, `src/cli.ts` + `src/classify.ts` + `src/index.ts` stubs, `test/classify.spec.ts` smoke covering empty-input and not-yet-implemented guards.

## [0.1.0] — planned

Initial public release. Will include: rules engine for Art 5 / 6+Annex III / 10 / 13 / 14 / 15 / 50, three-category overlay synced from the Lucairn website's compliance checklist source-of-truth, 50-case bilingual fixture dataset, CLI surface, hosted UI mirror at `lucairn.eu/tools/ai-act-classifier`. Release date will be backfilled at publish time.
