node_modules/
dist/
coverage/
.vitest-cache/
.tsbuildinfo
.DS_Store
.env
.env.local
*.log

# Accuracy harness machine-readable artifact (only the .md is committed).
accuracy/REPORT.json
accuracy/REPORT.llm-anthropic.json

# Internal product-requirements documents (anchoring artifacts for build sessions).
# Kept locally for fresh-context resumes; not part of the public source tree.
specs/
